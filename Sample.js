const sentence = prompt("Enter a sentence:");
const selectedWord = prompt("Enter a word from the sentence:");

const lowerCaseSentence = sentence.toLowerCase();
const lowerCaseSelectedWord = selectedWord.toLowerCase();

     if (lowerCaseSentence.includes(lowerCaseSelectedWord)){
         document.write("Yes is Correct");
     } else {
         document.write(" Ths is Incorrect");
     }]oninputcxz